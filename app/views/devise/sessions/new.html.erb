<h2 class="h2">Iniciar Sesión</h2>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-group">
    <%= f.email_field :email, autofocus: true, class: "form-control input-lg", placeholder: "Correo Electrónico" %>
  </div>

  <div class="form-group">
    <%= f.password_field :password, autocomplete: "off", class: "form-control input-lg", placeholder: "Contraseña" %>
  </div>

  <%# if devise_mapping.rememberable? -%>
  <div class="checkbox ck">
    <label>
      <%= f.check_box :remember_me %> Recordarme
    </label>
  </div>
  <%# end -%>

  <div class="btns">
    <%= f.submit "Iniciar Sesión", class: "btn btn-success form-btn" %>
    <%= link_to "Registrarse", sign_up_path, class: "btn btn-default form-btn" %>
  </div>

<% end %>
