<h2 class="h2">Editar Grupo</h2>

<%= render 'form', group: @group %>

<%= link_to 'Eliminar Grupo', group_path( @group.slug ), method: :delete, class: "btn btn-lg btn-danger submit-btn", data: { confirm: "¿Seguro? Esto eliminará el grupo." } %>
